# 🤖 Auto-Task Agent: 基于 ReAct 架构的轻量级自主智能体

![Python](https://img.shields.io/badge/Python-3.10%2B-blue)
![Node.js](https://img.shields.io/badge/Node.js-18.x-green)
![LLM](https://img.shields.io/badge/LLM-OpenAI%20%2F%20DeepSeek-orange)
![Generated By](https://img.shields.io/badge/Generated_by-Gemini-blueviolet)

## 📖 项目简介
**Auto-Task Agent** 是一个代码 **100% 由 Google Gemini 辅助生成**的轻量级自主任务智能体。本项目是一次成功的“AI 驱动开发（AI-Driven Development）”实践。

本项目摒弃了 LangChain 等厚重框架，直接基于大模型原生 Function Calling 能力构建了底层的 **ReAct (Reasoning + Acting)** 状态机循环。通过精准的提示词工程与架构拆解，项目成功引导 AI 独立完成了从前端 API 网关到后端业务逻辑的跨语言微服务构建。

它可以根据用户的自然语言指令，自主进行任务拆解、逻辑推理，并动态调用本地工具库（Skills）来完成复杂的闭环任务（如：自动化前沿学术检索、总结并发送邮件报告）。

### ✨ 核心技术亮点 (Why this project?)
- 🧠 **AI 驱动的 ReAct 核心引擎**：由 Gemini 完整构建的底层架构，完美实现了 `Thought -> Action -> Observation` 上下文链路，展现了原生大模型对 Agent 调度逻辑的深刻掌控。
- 🔌 **极致解耦的 Skill 架构**：基于大模型 JSON Schema 与 Python `**kwargs` 动态解包技术，实现调度中心与底层业务逻辑的完全解耦。新增技能符合**开闭原则 (OCP)**，核心引擎零修改。
- 🌐 **跨语言微服务设计**：后端采用 Python 驱动核心 Agent，前端通过 Node.js (Express) 构建轻量级 API Gateway，通过 IPC 进程间通信实现流式调度。
- 📦 **工业级工程规范**：每个 Skill 均作为独立模块封装，配备完善的 `SKILL.md` 文档与单元测试，具备极强的可插拔性和扩展性。

---

## 🏗️ 项目架构与目录树

```text
auto-task-agent/
├── backend/                  # 🐍 Python 核心引擎层 (Gemini 生成)
│   ├── core/                 # 调度大脑
│   │   └── agent.py          # ReAct 循环与动态 Tool Calling 实现
│   ├── skills/               # 独立技能挂载区
│   │   ├── arxiv_search/     # Skill: 学术论文检索
│   │   └── email_sender/     # Skill: SMTP 邮件自动发送
│   ├── main.py               # 后端进程入口
│   └── requirements.txt      # Python 依赖清单
├── gateway/                  # 🟢 Node.js API 网关层 (Gemini 生成)
│   ├── index.js              # Express 服务与子进程调度
│   └── package.json
└── README.md


```

## 🚀 快速开始 (Quick Start)

### 1. 克隆项目

```bash
git clone [https://github.com/YOUR_USERNAME/auto-task-agent.git](https://github.com/YOUR_USERNAME/auto-task-agent.git)
cd auto-task-agent

```

### 2. 配置环境变量

在项目根目录下创建 `.env` 文件，并填入以下配置：

```text
# 大模型 API 配置
OPENAI_API_KEY=your_api_key_here
BASE_URL=[https://api.deepseek.com/v1](https://api.deepseek.com/v1)

# 邮件发送技能配置
SMTP_SERVER=smtp.qq.com
SMTP_PORT=465
SENDER_EMAIL=your_email@example.com
SENDER_PASSWORD=your_smtp_auth_code

```

### 3. 初始化 Python 核心引擎

```bash
cd backend
python -m venv venv
source venv/Scripts/activate
pip install -r requirements.txt

```

### 4. 启动 Node.js API 网关

打开一个新的终端窗口：

```bash
cd gateway
npm install
node index.js

```

---

## 🎯 使用用例 (Demo)

向网关发送 POST 请求，观察 Agent 是如何自主思考并连续调用工具的：

```bash
curl -X POST http://localhost:3000/api/task \
-H "Content-Type: application/json" \
-d '{"prompt": "帮我调研一下关于 Agentic Workflow 的最新论文，提取摘要。然后把这份报告发送到 test@example.com"}'

```

Created with ❤️ by Corleone & 🤖 Powered by Gemini.


